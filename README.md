# Dazi-Learn-Docs

基于Docusaurus的可扩展技术学习与做题平台。当前包含AWS-CLF-C02知识文档和719题练习模式。

## 本地运行

```bash
npm install
npm start
```

## 构建

```bash
npm run build
npm run serve
```

Cloudflare-Pages使用构建命令`npm run build`和输出目录`build`。GitHub-Pages与Cloudflare-Pages工作流已位于`.github/workflows`。

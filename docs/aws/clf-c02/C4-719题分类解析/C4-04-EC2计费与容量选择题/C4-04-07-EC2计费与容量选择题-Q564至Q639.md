---
id: C4-04-07-EC2计费与容量选择题-Q564至Q639
sidebar_position: 7
---

# C4-04-07-EC2计费与容量选择题-Q564至Q639

> 本篇是《C4-04-EC2计费与容量选择题》的第7个分篇，主要包含：EC2计费与容量选择题、EC2计费与容量选择题、EC2计费与容量选择题、EC2计费与容量选择题、EC2计费与容量选择题、EC2计费与容量选择题、EC2计费与容量选择题。

## Q564-EC2计费与容量选择题

**题目：** Which Amazon EC2 Reserved Instances term commitment will give users the MOST cost savings?。

**选项：**

- A. 1 year
- B. 2 years
- C. 3 years
- D. 5 years

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `C`。

**需求/考点标签：** Cost Optimization。

**社区投票：** C 100%

### Q564.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**Cost Optimization**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q564.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | 1 year | 该选项是题库中的概念/服务描述。现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。 | 题库记录为非正确选项 |
| B | 2 years | 该选项是题库中的概念/服务描述。现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。 | 题库记录为非正确选项 |
| C | 3 years | 该选项是题库中的概念/服务描述。现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。 | 题库记录为正确选项 |
| D | 5 years | 该选项是题库中的概念/服务描述。现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。 | 题库记录为非正确选项 |

### Q564.3-为什么题库会这样选

题库记录的正确选项定位为：该选项是题库中的概念/服务描述。

现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

---

## Q565-EC2计费与容量选择题

**题目：** A company is running big data analytics and massive parallel computations on its AWS test and development servers. The company can tolerate occasional downtime. What is the MOST cost-effective Amazon EC2 purchasing option for the company to use?

**选项：**

- A. On-Demand Instances
- B. Spot Instances
- C. Reserved Instances
- D. Savings Plans

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `B`。

**需求/考点标签：** Cost Optimization。

**社区投票：** B 100%

### Q565.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**Cost Optimization**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q565.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | On-Demand Instances | On-Demand Instances：无长期承诺、按使用付费的计算购买方式。 | 题库记录为非正确选项 |
| B | Spot Instances | Spot Instances：利用 AWS 闲置计算容量的低价实例，但可能被中断。 | 题库记录为正确选项 |
| C | Reserved Instances | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为非正确选项 |
| D | Savings Plans | Savings Plans：承诺一定计算消费金额换取折扣的定价模型。 | 题库记录为非正确选项 |

### Q565.3-为什么题库会这样选

题库记录的正确选项定位为：Spot Instances：利用 AWS 闲置计算容量的低价实例，但可能被中断。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

---

## Q566-EC2计费与容量选择题

**题目：** A company runs Amazon EC2 instances in a research lab. The instances run for 3 hours each week and cannot be interrupted. What is the MOST cost-effective instance purchasing option to meet these requirements?。

**选项：**

- A. Compute Savings Plan
- B. On-Demand Instances
- C. Convertible Reserved Instances
- D. Spot Instances

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `B`。

**需求/考点标签：** 可中断低成本计算。

**社区投票：** B 73%, A 18%, C 9%。

### Q566.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**可中断低成本计算**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q566.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Compute Savings Plan | Savings Plans：承诺一定计算消费金额换取折扣的定价模型。 | 题库记录为非正确选项 |
| B | On-Demand Instances | On-Demand Instances：无长期承诺、按使用付费的计算购买方式。 | 题库记录为正确选项 |
| C | Convertible Reserved Instances | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为非正确选项 |
| D | Spot Instances | Spot Instances：利用 AWS 闲置计算容量的低价实例，但可能被中断。 | 题库记录为非正确选项 |

### Q566.3-为什么题库会这样选

题库记录的正确选项定位为：On-Demand Instances：无长期承诺、按使用付费的计算购买方式。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

---

## Q569-EC2计费与容量选择题

**题目：** A company will run a predictable compute workload on Amazon EC2 instances for the next 3 years. The workload is critical for the company. The company wants to optimize costs to run the workload. Which solution will meet these requirements?

**选项：**

- A. Spot Instances
- B. Dedicated Hosts
- C. Savings Plans
- D. On-Demand Instances

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `C`。

**需求/考点标签：** 稳定长期计算。

**社区投票：** C 100%

### Q569.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**稳定长期计算**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q569.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Spot Instances | Spot Instances：利用 AWS 闲置计算容量的低价实例，但可能被中断。 | 题库记录为非正确选项 |
| B | Dedicated Hosts | Dedicated Hosts：客户专用物理服务器，适合许可证/合规等硬件隔离要求。 | 题库记录为非正确选项 |
| C | Savings Plans | Savings Plans：承诺一定计算消费金额换取折扣的定价模型。 | 题库记录为正确选项 |
| D | On-Demand Instances | On-Demand Instances：无长期承诺、按使用付费的计算购买方式。 | 题库记录为非正确选项 |

### Q569.3-为什么题库会这样选

题库记录的正确选项定位为：Savings Plans：承诺一定计算消费金额换取折扣的定价模型。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

---

## Q578-EC2计费与容量选择题

**题目：** A company needs to purchase Amazon EC2 instances to support an application that will run continuously for more than 1 year. Which EC2 instance purchasing option meets these requirements MOST cost-effectively?。

**选项：**

- A. Dedicated Instances
- B. Spot Instances
- C. Reserved Instances
- D. On-Demand Instances

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `C`。

**需求/考点标签：** Cost Optimization。

**社区投票：** C 100%

### Q578.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**Cost Optimization**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q578.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Dedicated Instances | 该选项是题库中的概念/服务描述。现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。 | 题库记录为非正确选项 |
| B | Spot Instances | Spot Instances：利用 AWS 闲置计算容量的低价实例，但可能被中断。 | 题库记录为非正确选项 |
| C | Reserved Instances | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为正确选项 |
| D | On-Demand Instances | On-Demand Instances：无长期承诺、按使用付费的计算购买方式。 | 题库记录为非正确选项 |

### Q578.3-为什么题库会这样选

题库记录的正确选项定位为：Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

---

## Q634-EC2计费与容量选择题

**题目：** A company has a workload that will run continuously for 1 year. The workload cannot tolerate service interruptions. Which Amazon EC2 purchasing option will be MOST cost-effective?。

**选项：**

- A. All Upfront Reserved Instances
- B. Partial Upfront Reserved Instances
- C. Dedicated Instances
- D. On-Demand Instances

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `A`。

**需求/考点标签：** 可中断低成本计算。

**社区投票：** A 100%

### Q634.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**可中断低成本计算**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q634.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | All Upfront Reserved Instances | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为正确选项 |
| B | Partial Upfront Reserved Instances | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为非正确选项 |
| C | Dedicated Instances | 该选项是题库中的概念/服务描述。现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。 | 题库记录为非正确选项 |
| D | On-Demand Instances | On-Demand Instances：无长期承诺、按使用付费的计算购买方式。 | 题库记录为非正确选项 |

### Q634.3-为什么题库会这样选

题库记录的正确选项定位为：Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

---

## Q639-EC2计费与容量选择题

**题目：** A company wants a cost-effective option when running its applications in an Amazon EC2 instance for short time periods. The applications can be interrupted. Which EC2 instance type will meet these requirements?。

**选项：**

- A. Spot Instances
- B. On-Demand Instances
- C. Reserved Instances
- D. Dedicated Instances

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `A`。

**需求/考点标签：** 可中断低成本计算。

### Q639.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**可中断低成本计算**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q639.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Spot Instances | Spot Instances：利用 AWS 闲置计算容量的低价实例，但可能被中断。 | 题库记录为正确选项 |
| B | On-Demand Instances | On-Demand Instances：无长期承诺、按使用付费的计算购买方式。 | 题库记录为非正确选项 |
| C | Reserved Instances | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为非正确选项 |
| D | Dedicated Instances | 该选项是题库中的概念/服务描述。现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。 | 题库记录为非正确选项 |

### Q639.3-为什么题库会这样选

题库记录的正确选项定位为：Spot Instances：利用 AWS 闲置计算容量的低价实例，但可能被中断。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

## 本篇概述

- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。

[返回本章总述](./C4-04-00-章节总述.md)｜[查看本章概述](./C4-04-99-章节概述.md)

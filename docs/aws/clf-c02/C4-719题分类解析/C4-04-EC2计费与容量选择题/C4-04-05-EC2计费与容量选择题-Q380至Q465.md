---
id: C4-04-05-EC2计费与容量选择题-Q380至Q465
sidebar_position: 5
---

# C4-04-05-EC2计费与容量选择题-Q380至Q465

> 本篇是《C4-04-EC2计费与容量选择题》的第5个分篇，主要包含：EC2计费与容量选择题、EC2计费与容量选择题、EC2计费与容量选择题、EC2计费与容量选择题、EC2计费与容量选择题、EC2计费与容量选择题。

## Q380-EC2计费与容量选择题

**题目：** Which AWS resource can help a company reduce its costs in exchange for a usage commitment when using Amazon EC2 instances?。

**选项：**

- A. Compute Savings Plans
- B. Auto Scaling group
- C. On-Demand Instance
- D. EC2 instance store

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `A`。

**社区投票：** A 100%

### Q380.1-题干怎么拆

现有题库没有提供独立 explanation。

这里按题干中的动作、对象和所有选项的服务定位进行比较。

### Q380.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Compute Savings Plans | Savings Plans：承诺一定计算消费金额换取折扣的定价模型。 | 题库记录为正确选项 |
| B | Auto Scaling group | EC2 Auto Scaling：根据需求自动增加或减少 EC2 容量。 | 题库记录为非正确选项 |
| C | On-Demand Instance | On-Demand Instances：无长期承诺、按使用付费的计算购买方式。 | 题库记录为非正确选项 |
| D | EC2 instance store | EC2 Instance Store：与 EC2 宿主硬件关联的临时本地块存储。Amazon EC2：可配置虚拟服务器，适合需要 OS/实例级控制的计算工作负载。 | 题库记录为非正确选项 |

### Q380.3-为什么题库会这样选

题库记录的正确选项定位为：Savings Plans：承诺一定计算消费金额换取折扣的定价模型。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

---

## Q411-EC2计费与容量选择题

**题目：** Which feature of Amazon RDS provides the ability to automatically create a primary database instance and to synchronously replicate data to an instance in another Availability Zone?。

**选项：**

- A. Read replicas
- B. Blue/green deployment
- C. Multi-AZ deployment
- D. Reserved Instances

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `C`。

**需求/考点标签：** 关系数据库高可用。

**社区投票：** C 100%

### Q411.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**关系数据库高可用**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q411.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Read replicas | 该选项是题库中的概念/服务描述。现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。 | 题库记录为非正确选项 |
| B | Blue/green deployment | 该选项是题库中的概念/服务描述。现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。 | 题库记录为非正确选项 |
| C | Multi-AZ deployment | Availability Zone 是 Region 内相互隔离的故障域/部署位置。 | 题库记录为正确选项 |
| D | Reserved Instances | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为非正确选项 |

### Q411.3-为什么题库会这样选

题库记录的正确选项定位为：Availability Zone 是 Region 内相互隔离的故障域/部署位置。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

---

## Q429-EC2计费与容量选择题

**题目：** A company is running a reporting web server application on Amazon EC2 instances. The application runs once every week and once again at the end of the month. The EC2 instances can be shut down when they are not in use. What is the MOST cost-effective billing model for this use case?

**选项：**

- A. Standard Reserved Instances
- B. Convertible Reserved Instances
- C. On-Demand Capacity Reservations
- D. On-Demand Instances

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `D`。

**需求/考点标签：** Cost Optimization。

**社区投票：** D 67%, C 33%。

### Q429.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**Cost Optimization**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q429.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Standard Reserved Instances | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为非正确选项 |
| B | Convertible Reserved Instances | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为非正确选项 |
| C | On-Demand Capacity Reservations | Capacity Reservations：为特定 AZ 预留 EC2 计算容量，不等同于折扣承诺。On-Demand Instances：无长期承诺、按使用付费的计算购买方式。 | 题库记录为非正确选项 |
| D | On-Demand Instances | On-Demand Instances：无长期承诺、按使用付费的计算购买方式。 | 题库记录为正确选项 |

### Q429.3-为什么题库会这样选

题库记录的正确选项定位为：On-Demand Instances：无长期承诺、按使用付费的计算购买方式。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

### Q429.4-⚠-争议/版本提示

- 社区投票分散，最高项仅 67%
- 本题同时收录到 `C4-27-旧题争议题与当前AWS差异.md`，不在这里强行替题库修正答案。

---

---

## Q431-EC2计费与容量选择题

**题目：** A company is moving its development and test environments to AWS to increase agility and reduce cost. Because these are not production workloads and the servers are not fully utilized, occasional unavailability is acceptable. What is the MOST cost-effective Amazon EC2 pricing model that will meet these requirements?

**选项：**

- A. Reserved Instances
- B. On-Demand Instances
- C. Spot Instances
- D. Dedicated Hosts

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `C`。

**需求/考点标签：** Agility / Cost Optimization。

**社区投票：** C 100%

### Q431.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**Agility、Cost Optimization**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q431.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Reserved Instances | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为非正确选项 |
| B | On-Demand Instances | On-Demand Instances：无长期承诺、按使用付费的计算购买方式。 | 题库记录为非正确选项 |
| C | Spot Instances | Spot Instances：利用 AWS 闲置计算容量的低价实例，但可能被中断。 | 题库记录为正确选项 |
| D | Dedicated Hosts | Dedicated Hosts：客户专用物理服务器，适合许可证/合规等硬件隔离要求。 | 题库记录为非正确选项 |

### Q431.3-为什么题库会这样选

题库记录的正确选项定位为：Spot Instances：利用 AWS 闲置计算容量的低价实例，但可能被中断。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

---

## Q462-EC2计费与容量选择题

**题目：** A company wants to use Amazon EC2 instances for a stable production workload that will run for 1 year. Which instance purchasing option meets these requirements MOST cost-effectively?。

**选项：**

- A. Dedicated Hosts
- B. Reserved Instances
- C. On-Demand Instances
- D. Spot Instances

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `B`。

**需求/考点标签：** Cost Optimization。

**社区投票：** B 100%

### Q462.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**Cost Optimization**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q462.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Dedicated Hosts | Dedicated Hosts：客户专用物理服务器，适合许可证/合规等硬件隔离要求。 | 题库记录为非正确选项 |
| B | Reserved Instances | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为正确选项 |
| C | On-Demand Instances | On-Demand Instances：无长期承诺、按使用付费的计算购买方式。 | 题库记录为非正确选项 |
| D | Spot Instances | Spot Instances：利用 AWS 闲置计算容量的低价实例，但可能被中断。 | 题库记录为非正确选项 |

### Q462.3-为什么题库会这样选

题库记录的正确选项定位为：Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

---

## Q465-EC2计费与容量选择题

**题目：** A company wants a solution that will automatically adjust the number of Amazon EC2 instances that are being used based on the current load. Which AWS offering will meet these requirements?。

**选项：**

- A. Dedicated Hosts
- B. Placement groups
- C. Auto Scaling groups
- D. Reserved Instances

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `C`。

**社区投票：** C 100%

### Q465.1-题干怎么拆

现有题库没有提供独立 explanation。

这里按题干中的动作、对象和所有选项的服务定位进行比较。

### Q465.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Dedicated Hosts | Dedicated Hosts：客户专用物理服务器，适合许可证/合规等硬件隔离要求。 | 题库记录为非正确选项 |
| B | Placement groups | 该选项是题库中的概念/服务描述。现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。 | 题库记录为非正确选项 |
| C | Auto Scaling groups | EC2 Auto Scaling：根据需求自动增加或减少 EC2 容量。 | 题库记录为正确选项 |
| D | Reserved Instances | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为非正确选项 |

### Q465.3-为什么题库会这样选

题库记录的正确选项定位为：EC2 Auto Scaling：根据需求自动增加或减少 EC2 容量。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

## 本篇概述

- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。

[返回本章总述](./C4-04-00-章节总述.md)｜[查看本章概述](./C4-04-99-章节概述.md)

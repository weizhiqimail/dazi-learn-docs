---
id: 09-EC2计费与容量选择题-等
sidebar_position: 9
---

# C4-04-EC2计费与容量选择题-EC2计费与容量选择题与EC2计费与容量选择题与EC2计费与容量选择题与EC2计费与容量选择题

> 本篇是《C4-04-EC2计费与容量选择题》的第9个分篇，主要包含：EC2计费与容量选择题、EC2计费与容量选择题、EC2计费与容量选择题、EC2计费与容量选择题。

## Q697-EC2计费与容量选择题

**题目：** A company purchased Amazon EC2 Standard Reserved Instances (RIs) for a workload in the AWS Cloud. The company needs to move part of the workload to an instance family that does not match the instance family of these Standard RIs. How can the company take advantage of the Standard RIs that it no longer needs?

**选项：**

- A. Contact the AWS Support team, and ask the team to sell the Standard RIs
- B. Sell the Standard RIs on the Amazon EC2 Reserved Instance Marketplace
- C. Sell the Standard RIs as a third-party seller on the AWS Marketplace
- D. Convert the Standard RIs to Savings Plans

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `B`。

**需求/考点标签：** Cost Optimization。

**社区投票：** B 100%

### Q697.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**Cost Optimization**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q697.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Contact the AWS Support team, and ask the team to sell the Standard RIs | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。AWS Support：AWS 技术支持与客户服务计划体系。项目资料明确提示当前计划体系有更新。 | 题库记录为非正确选项 |
| B | Sell the Standard RIs on the Amazon EC2 Reserved Instance Marketplace | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。AWS Marketplace：查找、购买和部署第三方软件、数据与服务的数字目录。Amazon EC2：可配置虚拟服务器，适合需要 OS/实例级控制的计算工作负载。 | 题库记录为正确选项 |
| C | Sell the Standard RIs as a third-party seller on the AWS Marketplace | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。AWS Marketplace：查找、购买和部署第三方软件、数据与服务的数字目录。 | 题库记录为非正确选项 |
| D | Convert the Standard RIs to Savings Plans | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。Savings Plans：承诺一定计算消费金额换取折扣的定价模型。 | 题库记录为非正确选项 |

### Q697.3-为什么题库会这样选

题库记录的正确选项定位为：Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。

AWS Marketplace：查找、购买和部署第三方软件、数据与服务的数字目录。

Amazon EC2：可配置虚拟服务器，适合需要 OS/实例级控制的计算工作负载。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

---

## Q701-EC2计费与容量选择题

**题目：** A company runs an uninterruptible Amazon EC2 workload on AWS 24 hours a day, 7 days a week. The company will require the same instance family and instance type to run the workload for the next 12 months. Which combination of purchasing options should the company choose to MOST optimize costs? (Choose two.)。

**选项：**

- A. Standard Reserved Instance
- B. Convertible Reserved Instance
- C. Compute Savings Plan
- D. Spot Instance
- E. All Upfront payment

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `AE`。

**需求/考点标签：** 可中断低成本计算。

**社区投票：** AE 100%

### Q701.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**可中断低成本计算**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q701.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Standard Reserved Instance | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为正确选项 |
| B | Convertible Reserved Instance | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为非正确选项 |
| C | Compute Savings Plan | Savings Plans：承诺一定计算消费金额换取折扣的定价模型。 | 题库记录为非正确选项 |
| D | Spot Instance | Spot Instances：利用 AWS 闲置计算容量的低价实例，但可能被中断。 | 题库记录为非正确选项 |
| E | All Upfront payment | 该选项是题库中的概念/服务描述。现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。 | 题库记录为正确选项 |

### Q701.3-为什么题库会这样选

题库记录的正确选项定位为：Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。

该选项是题库中的概念/服务描述。

现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

### Q701.4-⚠-争议/版本提示

- 社区评论存在明显质疑/版本差异信号
- 本题同时收录到 `C4-27-旧题争议题与当前AWS差异.md`，不在这里强行替题库修正答案。

---

---

## Q711-EC2计费与容量选择题

**题目：** A company needs to provision uninterruptible Amazon EC2 instances, when needed, and pay for compute capacity by the second. Which EC2 instance purchasing option will meet these requirements?。

**选项：**

- A. Reserved Instances
- B. Spot Instances
- C. On-Demand Instances
- D. Dedicated Instances

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `C`。

**需求/考点标签：** 可中断低成本计算。

**社区投票：** C 100%

### Q711.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**可中断低成本计算**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q711.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Reserved Instances | Reserved Instances：通过 1/3 年承诺获得折扣的 EC2/RDS 等预留定价概念，具体机制需按服务区分。 | 题库记录为非正确选项 |
| B | Spot Instances | Spot Instances：利用 AWS 闲置计算容量的低价实例，但可能被中断。 | 题库记录为非正确选项 |
| C | On-Demand Instances | On-Demand Instances：无长期承诺、按使用付费的计算购买方式。 | 题库记录为正确选项 |
| D | Dedicated Instances | 该选项是题库中的概念/服务描述。现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。 | 题库记录为非正确选项 |

### Q711.3-为什么题库会这样选

题库记录的正确选项定位为：On-Demand Instances：无长期承诺、按使用付费的计算购买方式。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

---

## Q714-EC2计费与容量选择题

**题目：** A company wants to run its application on Amazon EC2 instances. The company needs to keep the application on-premises to meet a compliance requirement. Which AWS offering will meet these requirements?。

**选项：**

- A. Dedicated Instances
- B. Amazon CloudFront
- C. AWS Fargate
- D. AWS Outposts

**主分类：** C4-04 EC2计费与容量选择题。

**题库记录答案：** `D`。

**需求/考点标签：** 配置变化与合规。

**社区投票：** D 100%

### Q714.1-题干怎么拆

这道题先识别的不是某个服务名，而是这些约束：**配置变化与合规**。

判断时应该先找到与这些约束同层、同数据模型或同运维目标的服务，再排除“看起来也属于 AWS，但解决的是另一个层面问题”的选项。

### Q714.2-每个选项的定位

| 选项 | 题库内容 | 服务/概念定位 | 题库答案关系 |
|---|---|---|---|
| A | Dedicated Instances | 该选项是题库中的概念/服务描述。现有源资料未提供独立 explanation，需要结合题干目标与其他选项定位比较。 | 题库记录为非正确选项 |
| B | Amazon CloudFront | Amazon CloudFront：AWS CDN，利用 Edge Location 缓存和分发内容。 | 题库记录为非正确选项 |
| C | AWS Fargate | AWS Fargate：为 ECS/EKS 提供不需要管理底层服务器的容器计算能力。 | 题库记录为非正确选项 |
| D | AWS Outposts | AWS Outposts：把 AWS 基础设施和服务运行到客户本地场所。 | 题库记录为正确选项 |

### Q714.3-为什么题库会这样选

题库记录的正确选项定位为：AWS Outposts：把 AWS 基础设施和服务运行到客户本地场所。

它与题干要求的对象/约束直接对应。

其余选项虽然可能也属于同一大类，但主要解决不同问题。

---

## 本篇概述

- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。
- 本篇梳理了EC2计费与容量选择题相关的核心知识、适用场景与判断要点。

[返回本章总述](./00-总述.md)｜[查看本章概述](./99-概述.md)
